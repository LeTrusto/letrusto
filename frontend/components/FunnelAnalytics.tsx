"use client";

// Route-specific funnel tracking was removed with the old business routes.
// Add new safe, consented event tracking here as the new domain is designed.
export default function FunnelAnalytics() {
  return null;
}
